
import { productFilter } from 'ember-ecommerce/helpers/product-filter';
import { module, test } from 'qunit';

module('Unit | Helper | product filter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productFilter([42]);
  assert.ok(result);
});

